<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="16dp"
    android:gravity="center_vertical"
    android:background="@color/section_header_background">

    <!-- Section Title -->
    <TextView
        android:id="@+id/section_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="Event Photos (73)"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@color/section_header_text"
        android:gravity="start|center_vertical" />

    <!-- Select All Button -->
    <Button
        android:id="@+id/btn_select_all"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:text="Select All"
        android:textSize="14sp"
        android:textAllCaps="false"
        android:background="@drawable/button_select_all_background"
        android:textColor="@color/select_all_button_text"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:layout_marginStart="8dp" />

</LinearLayout>